# DECISIONS.md

설계 과정에서 고민했던 주요 기술적 쟁점과 선택의 근거를 정리합니다.
각 쟁점은 해당 기능을 구현하면서 실제 테스트 결과를 바탕으로 작성하였습니다.

---

## 쟁점 1. 재고 관리 전략

> 10개 한정 상품에 1000TPS가 몰릴 때, 초과판매 없이 빠르게 재고를 차감하는 방법

### Step 1. DB 비관적 락 (`SELECT ... FOR UPDATE`)

**구현 방식**

- `@Lock(LockModeType.PESSIMISTIC_WRITE)`로 재고 row에 쓰기 락
- 트랜잭션 안에서 잔여 재고 확인 후 1개 차감
- 잔여 재고 0이면 `STOCK_SOLD_OUT` 예외 처리

**검증 조건**

| 항목 | 값 |
|------|---|
| 테스트 | `StockServiceConcurrencyTest.pessimisticLockPreventsOverselling` |
| DB | MySQL 8.0 (Testcontainers) |
| 초기 재고 | 10 |
| 동시 요청 | 100, 1000 |
| 실행 스레드 | 64 |

**결과**

| 요청 수 | 성공 | 실패 | 초과판매 | 총 소요 | 평균 | P95 | P99 | 최대 |
|---------|------|------|----------|---------|------|-----|-----|------|
| 100 | 10 | 90 | 없음 | 635ms | 317ms | 445ms | 452ms | 452ms |
| 1000 | 10 | 990 | 없음 | 2464ms | 156ms | 326ms | 470ms | 699ms |

**판단**

비관적 락은 구현이 단순하고 초과판매를 막는 기준선으로 적합하다.
다만 개별 요청 기준 평균 156~317ms, 최대 699ms로 락 대기 비용이 발생한다.
모든 요청이 DB 커넥션을 점유하고 하나의 row에 순차 대기하므로, 분산 환경에서는 커넥션 고갈 위험이 있다.
다음 단계에서는 같은 조건으로 낙관적 락 방식을 비교하여 락 대기 감소 효과와 재시도 비용을 확인한다.

### Step 2. DB 낙관적 락 (`@Version` + 재시도)

**구현 방식**

- `@Version` 기반 낙관적 락으로 충돌 감지
- `StockFacade`에서 재시도 루프 (최대 100회, 랜덤 backoff 0~10ms)
- 충돌 시 `ObjectOptimisticLockingFailureException` → 재시도, 소진 시 `STOCK_SOLD_OUT`

**검증 조건**

| 항목 | 값 |
|------|---|
| 테스트 | `StockServiceConcurrencyTest.OptimisticLock.preventsOverselling` |
| DB | MySQL 8.0 (Testcontainers) |
| 초기 재고 | 10 |
| 동시 요청 | 100, 1000 |
| 실행 스레드 | 64 |
| 최대 재시도 | 100회 |

**결과**

| 요청 수 | 성공 | 실패 | 초과판매 | 총 소요 | 평균 | P95 | P99 | 최대 |
|---------|------|------|----------|---------|------|-----|-----|------|
| 100 | 10 | 90 | 없음 | 884ms | 486ms | 786ms | 881ms | 881ms |
| 1000 | 10 | 990 | 없음 | 2825ms | 175ms | 529ms | 713ms | 1016ms |

**비관적 락 vs 낙관적 락 비교**

| 지표 (1000 요청) | 비관적 락 | 낙관적 락 |
|-----------------|----------|----------|
| 총 소요 | 1184ms | 2825ms |
| 평균 | 74ms | 175ms |
| P95 | 221ms | 529ms |
| 최대 | 392ms | 1016ms |

**판단**

낙관적 락은 경합이 낮은 환경에서는 락 대기 없이 효율적이지만, 재고 10개에 1000 요청이 몰리는 극심한 경합 상황에서는 비관적 락보다 오히려 느리다.
재시도마다 트랜잭션을 새로 열고 DB를 다시 조회하는 비용이 누적되며, 랜덤 backoff에 의한 sleep도 응답 시간을 증가시킨다.
이 결과는 "선착순 예약"처럼 단일 row에 경합이 집중되는 시나리오에서는 낙관적 락이 적합하지 않음을 보여준다.
다음 단계에서는 Redis Lua 스크립트로 DB 접근 자체를 줄이는 방식을 비교한다.

### Step 3. Redis Lua 스크립트

**구현 방식**

- Lua 스크립트로 재고 확인 + 차감을 원자적 처리
- Redis 단일 스레드 특성으로 동시성 제어 불필요
- DB 커넥션을 사용하지 않아 커넥션 풀 고갈 위험 없음

**검증 조건**

| 항목 | 값 |
|------|---|
| 테스트 | `StockServiceConcurrencyTest.RedisLua.preventsOverselling` |
| Redis | Redis 7 (Testcontainers) |
| 초기 재고 | 10 |
| 동시 요청 | 100, 1000 |
| 실행 스레드 | 64 |

**결과**

| 요청 수 | 성공 | 실패 | 초과판매 | 총 소요 | 평균 | P95 | P99 | 최대 |
|---------|------|------|----------|---------|------|-----|-----|------|
| 100 | 10 | 90 | 없음 | 265ms | 132ms | 204ms | 211ms | 211ms |
| 1000 | 10 | 990 | 없음 | 636ms | 38ms | 117ms | 134ms | 144ms |

**3가지 방식 비교 (1000 요청)**

| 지표 | 비관적 락 | 낙관적 락 | Redis Lua |
|------|----------|----------|-----------|
| 총 소요 | 1087ms | 2749ms | 636ms |
| 평균 | 67ms | 174ms | 38ms |
| P95 | 139ms | 663ms | 117ms |
| P99 | 196ms | 729ms | 134ms |
| 최대 | 251ms | 1161ms | 144ms |

**판단**

Redis Lua 스크립트는 DB 기반 방식 대비 모든 지표에서 우수하다.
DB 커넥션을 사용하지 않으므로 1000TPS 상황에서도 커넥션 풀 고갈 위험이 없고, 재고 소진 후에는 Redis에서 즉시 거절하므로 DB 부하가 발생하지 않는다.
Redis 단일 스레드 특성으로 요청 도착 순서대로 처리되어 공정성도 보장된다.

---

## 쟁점 2. 공정성 보장 방안

> Phase 5에서 작성 예정

---

## 쟁점 3. 결제 수단 확장성

> Phase 6에서 작성 예정

---

## 쟁점 4. 멱등성 처리 전략

> Phase 7에서 작성 예정

---

## 쟁점 5. Redis 장애 Fallback 전략

> Phase 8에서 작성 예정

---

## 쟁점 6. 고가용성 — 트래픽 급증 대응

> Phase 9에서 작성 예정

---

## 쟁점 7. 결제 실패 케이스별 대응 전략

> Phase 8에서 작성 예정

---

## 쟁점 8. 트랜잭션 범위 및 보상 전략

> Phase 7에서 작성 예정

---

## 쟁점 9. Redis 도입 근거 및 비용 대비 효과

> Phase 5에서 작성 예정

---

## 쟁점 10. 라이브러리 도입 사유

> 각 라이브러리 도입 시점에 작성 예정
